@page
@model Zad3.Pages.IndexModel
@{
}
<div class="card-columns">
    @foreach (var product in Model.ProductDB)
    {
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">@product.title</h5>
            </div>
        </div>
    }
</div>
